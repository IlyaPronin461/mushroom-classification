from setuptools import setup, find_packages

setup(
    name="mushroom_classification",
    version="1.0",
    packages=find_packages(),
    install_requires=[
        # Здесь можно указать зависимости, но они уже в requirements.txt
    ],
)